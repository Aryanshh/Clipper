services:
  - type: web
    name: viral-ai-clipper
    env: docker
    plan: free
    envVars:
      - key: GEMINI_API_KEY
        sync: false
        placeholder: "Enter your Gemini API Key"
      - key: NODE_ENV
        value: production
